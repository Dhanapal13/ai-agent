# ai-agent

Run via API

Run Agent directly
    - docker compose -f docker-compose.observability.yml up -d   # Prometheus + Grafana
    - python main_agent.py owner/repo 123 https://github.com/owner/repo/pull/123